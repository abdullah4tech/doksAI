# Environment variables for the RAG Pipeline Frontend

# API Configuration
VITE_API_BASE_URL=http://localhost:5000

# Optional: Custom settings (will use defaults if not specified)
# VITE_DEFAULT_CHUNK_SIZE=1000
# VITE_DEFAULT_CHUNK_OVERLAP=200
# VITE_DEFAULT_TOP_K=8